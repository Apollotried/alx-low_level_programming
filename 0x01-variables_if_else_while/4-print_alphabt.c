#include <stdio.h>
/**
 * main - prints the alphabets
 * Return: 0
 */
int main(void)
{
	int i;
	int j;

	for (i = 97 ; i < 101 && i != 101 && i != 113 ; i++)
	{
		putchar(i);
	};
	putchar('\n');
	return (0);
}
