#include "main.h"
#include <string.h>
/**
 * *_strcat - cocnctainating
 * @dest: the dedtination string
 * @src: the source string
 * Return: char*
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
