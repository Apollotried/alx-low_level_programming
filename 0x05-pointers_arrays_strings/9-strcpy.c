#include "main.h"
#include <string.h>
/**
 * *_strcpy - makes a copy of a string
 * @dest: destination
 * @src: source
 * Return: char *
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
